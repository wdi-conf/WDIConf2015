<div class="home red-page hor-allign" id="home">
	<div class="default-container">
		<div data-sr class="hor-wrapper">
			<%= image_tag('logowhite.png', :class=> "home-logo span-6")%>
		</div>
		<div data-sr id="details-overview">

        <div class="span-4" id="melbourne">
          <h2>Melbourne</h2>
        </div>

        <div class="span-3" id="wdi-details">
          <h3>22nd May 2015</h3>
          <h4>20 Queen St, CBD</h4>
        </div>

      </div>

			<div id="bounce-arrow">
				<a data-scroll href="#about">
					<i class="fa fa-chevron-circle-down"></i>
				</a>
			</div>
	</div>
</div>


<%= render 'about' %>

<%= render 'schedule' %>

<%= render 'speakers' %>

<%= render 'sponsors' %>

<%= render 'team' %>

<%= render 'contact' %>

<!-- Join event modal -->
<%= render 'join' %>